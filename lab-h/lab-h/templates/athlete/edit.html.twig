{% extends 'base.html.twig' %}

{% block title %}Edytuj zawodnika{% endblock %}

{% block body %}
    <div class="card shadow-sm">
        <div class="card-body">
            <h1 class="h3 mb-3">Edytuj zawodnika</h1>

            {{ include('athlete/_form.html.twig', {'button_label': 'Zapisz zmiany'}) }}

            <div class="mt-3 d-flex gap-2 flex-wrap">
                <a href="{{ path('app_athlete_index') }}" class="btn btn-outline-secondary">
                    Powrót
                </a>

                {{ include('athlete/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
